# AGENTS.md

<!--
  Minimal project instructions for AI coding agents. Six blocks. Start here and add a
  section only when the project proves it needs one. Keep it short and concrete — a
  short accurate file beats a long vague one.

  This is the PROJECT layer: commands, layout, gotchas. The canonical behavior rules
  (the agent-instructions repo's AGENTS.md) go above these sections — either pasted in
  here, or imported via CLAUDE.md / GEMINI.md when you keep that file at the repo root.
-->

One sentence on what this project is and who it's for.

## Setup

- Install deps: `<command>`
- Run locally: `<command>`
- Environment: copy `.env.example` to `.env`; required vars are `<...>`

## Build, test, lint

- Build: `<command>`
- Test: `<command>` — run this before every commit
- Lint / format: `<command>`
- Typecheck: `<command>`

## Project layout

- `<dir>/` — <responsibility>
- `<dir>/` — <responsibility>

## Conventions

- <language/style choices that differ from the ecosystem default>
- <naming, error handling, or structural patterns to follow — name an example file>

## Do not touch

- `<path or kind of file>` — <why>
- Generated files: `<globs>`

## Done when

- Tests and lint pass, and <the behavior-level check that proves this task worked>.
